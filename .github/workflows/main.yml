name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on:
  push:
    branches:
      - 'release/**'

jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set output
        id: vars
        run: echo ::set-output name=ref::${GITHUB_REF#refs/*/}
        
      - name: Check output
        run: echo ${{ steps.vars.outputs.ref }}
        
      - uses: bhowell2/github-substring-action@v1.0.0
        id: branch
        with:
          value: ${{ steps.vars.outputs.ref }}
          index_of_str: "release/"
          run: echo ${{ steps.branch.outputs.substring }}

#       - name: Update gradle version
#         uses: chkfung/android-version-actions@v1.1
#         with:
#           gradlePath: pathway1/BasicsCodelab/app/build.gradle
#           versionName: 1.0.13
          
#       - name: Commit & Push
#         uses: Andro999b/push@v1.3
#         with:
#           github_token: ${{ secrets.TOKEN }}
#           branch: ${{ steps.vars.outputs.ref }}
#           force: true
#           message: '1.0.13 버전 변경'
